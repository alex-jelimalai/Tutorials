package sort;

public interface Sort {
	/**
	 * The sort represent just the principles of algorithm. In the same way it can be
	 * implemented for other primitive types or objects using Comparable
	 * interface
	 */
	void sort(int[] aArray);
}
